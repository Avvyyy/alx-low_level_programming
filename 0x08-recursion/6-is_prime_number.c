#include "main.h"

/**
 * is_prime_number - Function to test if a number is a prime number
 * n: Integer to be checked
 *
 * Return: 1(if integer is a prime number) else 0
 */

int is_prime_number(int n)
{
	if (is_prime_number (n % 2 == 0) && is_prime_number (n % 3 == 0)
