#include "main.h"

/**
 * rev_string - Function to print the a string reversed
 * @s: Pointer housing string to be reversed
 *
 * Return: none
 */

void rev_string(char *s)
{
	int 1 = 0;
	while (s[i] != '\0')
		i++;
	while (i > 0)
	{
		i--;
		_putchar(s);
	}
}
