#include <stdio.h>
#include <stdlib.h>

/**
 * main - program to print +ve single digits
 *
 * RETURN: 0 if successful
 */

int main(void)
{
	int a = 48;

	while (a < 58)
	{
		putchar(a);
		a++;
	}
	return (0);
}
